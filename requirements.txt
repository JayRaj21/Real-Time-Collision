# =============================================================================
# requirements.txt — Jetson Orin Nano (JetPack 5.x / 6.x · L4T aarch64)
#                    v2 — Florence-2 VLM edition
# =============================================================================
#
# IMPORTANT — READ BEFORE pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
#
# 1. PyTorch  ──  DO NOT install from PyPI.
#    The PyPI wheel is built for x86-64 and will either fail to install or run
#    without CUDA on aarch64.  Install the NVIDIA-provided Jetson wheel instead.
#    See README.md §2 for the exact download URL and install command.
#
# 2. OpenCV   ──  DO NOT install opencv-python from PyPI.
#    JetPack ships a GStreamer-enabled OpenCV build (python3-opencv).
#    The PyPI wheel lacks the nvarguscamerasrc GStreamer plugin support and
#    will break the CSI camera pipeline.
#    Install via: sudo apt install python3-opencv
#
# 3. Florence-2  ──  Requires trust_remote_code=True (custom modelling code
#    is downloaded from HuggingFace Hub on first run, ~900 MB total).
#    First-run download requires an internet connection.
#
# Install everything else with:
#    pip install -r requirements.txt
# =============================================================================

# Vision-Language Model (Florence-2) and HuggingFace tooling
transformers>=4.41.0
timm>=0.9.0          # Florence-2 vision encoder (DaViT) dependency
einops>=0.7.0        # required by Florence-2 custom modelling code

# Image bridge between OpenCV (NumPy) and Tkinter (PhotoImage)
Pillow>=10.0.0

# NumPy — pinned to avoid ABI mismatches with Jetson PyTorch wheel
numpy>=1.24.0,<2.0.0

# =============================================================================
# NOT listed here (must be installed separately — see README.md):
#   torch          → NVIDIA Jetson wheel (JetPack-specific build)
#   torchvision    → NVIDIA Jetson wheel
#   opencv-python  → system package  (sudo apt install python3-opencv)
# =============================================================================
